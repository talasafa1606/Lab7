version: '3.8'

networks:
  frontend-net:
    driver: bridge
  
  backend-net:
    driver: bridge
  
  db-net:
    driver: bridge

services:
  postgres:
    image: postgres:16
    container_name: education-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: educationdb
    ports:
      - "5432:5432"
    networks:
      - db-net

  courseadminservice:
    build:
      context: ./CourseAdminService
      dockerfile: Dockerfile
    container_name: courseadmin-service
    ports:
      - "8001:80"
    environment:
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=educationdb;Username=admin;Password=password
      - AuthService__Url=http://lab7
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - postgres
    networks:
      - frontend-net
      - backend-net
      - db-net
  
  lab7:
    build:
      context: ./Lab7
      dockerfile: Dockerfile
    container_name: auth-service
    ports:
      - "8002:80"
    environment:
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=educationdb;Username=admin;Password=password
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - postgres
    networks:
      - frontend-net
      - backend-net
      - db-net
  
  studentcourseenrollmentservice:
    build:
      context: ./StudentCourseEnrollmentService
      dockerfile: Dockerfile
    container_name: enrollment-service
    ports:
      - "8003:80"
    environment:
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=educationdb;Username=admin;Password=password
      - CourseAdminService__Url=http://courseadminservice
      - AuthService__Url=http://lab7
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - postgres
      - courseadminservice
      - lab7
    networks:
      - frontend-net
      - backend-net
      - db-net